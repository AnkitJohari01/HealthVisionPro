import fitz  # PyMuPDF

def extract_pdf_pages(file_bytes: bytes):
    """
    Extract text or images from PDF if needed later.
    For now, just return bytes (CrewAI handles the rest).
    """
    return file_bytes
